apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: crossplane
  namespace: crossplane-system
spec:
  releaseName: crossplane
  interval: 5m

  chart:
    spec:
      chart: crossplane
      version: "*"
      sourceRef:
        kind: HelmRepository
        name: crossplane-stable
        namespace: flux-system

  install:
    createNamespace: true

  upgrade:
    remediation:
      retries: 3

  # Optional customizations
  values:
    # Example: Install Crossplane providers automatically (optional)
    # provider:
    #   packages:
    #     - xpkg.upbound.io/upbound/provider-azure:v0.36.0